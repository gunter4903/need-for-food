services:
  postgres:
    ports: !reset []

  pgadmin:
    ports: !override ["127.0.0.1:5050:80"]

  mongodb:
    ports: !reset []

  backend:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      APP_BASE_URL: ${APP_BASE_URL}
    ports: !reset []

  caddy:
    image: caddy:2-alpine
    container_name: need-for-food-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ../need-for-food-website:/srv/website:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - backend

volumes:
  caddy_data:
  caddy_config:
